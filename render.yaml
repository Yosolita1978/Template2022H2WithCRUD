services:
  - name: server
    plan: free
    type: web
    env: node
    repo: https://github.com/Yosolita1978/Template2022H2WithCRUD
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: DB_URI
        fromDatabase:
          name: db
          property: connectionString

databases:
  - name: db
    plan: free
    ipAllowList: []
